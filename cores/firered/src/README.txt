Canonical FireRed source-of-truth stays in the existing root `src/` and `src_transformed/`
trees during the transition. This directory is the public-facing destination for
future core-local source moves.
